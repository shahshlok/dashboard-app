{
  "report_title": "string",
  "prepared_by": "string",
  "date": "YYYY-MM-DD",
  "opening_date": "YYYY-MM-DD",

  "executive_summary": {
    "market_opportunity": {
      "children_in_trade_area": "number",
      "trade_area_radius": "string",
      "age_range": "string",
      "citation": "string"
    },
    "demographics": {
      "median_household_income": "string",
      "income_comparison": "string",
      "child_poverty_rate": "string",
      "population_growth_since_2010": "string (optional - for new locations)",
      "young_population": "string (optional - for existing locations)",
      "extracurricular_participation": "string (optional - for existing locations)",
      "annual_spending": "string (optional - for existing locations)",
      "education_level": "string (optional - for new locations)",
      "citations": "string"
    },
    "competition_gap": {
      "little_gym_status": "string (optional - for new locations)",
      "market_description": "string",
      "waitlist_indicators": "string (optional - for existing locations)",
      "unique_offering": "string",
      "citation": "string"
    },
    "pricing_strategy": {
      "membership_range": "string",
      "benchmark_comparison": "string (optional - for new locations)",
      "no_price_hikes": "string (optional - for existing locations)",
      "family_discounts": "string (optional - for existing locations)",
      "customer_acquisition": "string (optional - for existing locations)",
      "average_membership_duration": "string (optional - for new locations)",
      "average_member_duration": "string (optional - for existing locations)",
      "monthly_fees": "string (optional - for existing locations)",
      "lifetime_value": "string",
      "target_customer_acquisition_cost": "string (optional - for new locations)",
      "acquisition_cost": "string (optional - for existing locations)",
      "ltv_cac_ratio": "string (optional - for existing locations)",
      "payback_period": "string",
      "citations": "string"
    },
    "location_advantages": {
      "traffic_count": "string",
      "co_anchor": "string (optional - for new locations)",
      "lease_rate": "string (optional - for existing locations)",
      "parking": "string (optional - for existing locations)",
      "citation": "string"
    },
    "current_performance": {
      "projection": "string (optional - for existing locations)",
      "goal": "string (optional - for existing locations)",
      "citation": "string (optional - for existing locations)"
    }
  },

  "market_demographics": {
    "population_data": {
      "5_mile_radius": "number (optional)",
      "3_mile_radius": "string (optional)",
      "7_mile_radius": "number (optional - for existing locations)",
      "7_mile_children_0_14": "number (optional - for new locations)",
      "population_percentage": "string",
      "national_comparison": "string (optional - for new locations)",
      "households": "number (optional - for existing locations)",
      "citations": "string"
    },
    "population_growth": {
      "growth_rate": "string (optional - for existing locations)",
      "area_description": "string (optional - for existing locations)",
      "howard_county_growth": "string (optional - for existing locations)",
      "stability": "string (optional - for existing locations)",
      "maryland_statewide": "string (optional - for existing locations)",
      "median_age": "string (optional - for existing locations)",
      "citations": "string (optional - for existing locations)"
    },
    "household_composition": {
      "average_household_size": "string (optional - for new locations)",
      "comparison": "string (optional - for new locations)",
      "under_18_population": "string (optional - for new locations)",
      "family_formation": "string (optional - for new locations)",
      "birth_rates": "string (optional - for new locations)",
      "households_with_children": "string (optional - for existing locations)",
      "workforce": "string",
      "education_quality": "string (optional - for existing locations)",
      "education_levels": "string (optional - for existing locations)",
      "diversity": "string (optional - for existing locations)",
      "citations": "string"
    },
    "income_spending": {
      "ashburn_median_income": "string (optional - for Ashburn)",
      "columbia_median_income": "string (optional - for Columbia)",
      "loudoun_county_median": "string (optional - for new locations)",
      "5_mile_radius_median": "string (optional - for existing locations)",
      "per_capita_income": "string (optional - for new locations)",
      "high_earners": "string (optional - for existing locations)",
      "low_income": "string (optional - for existing locations)",
      "us_comparison": "string (optional - for new locations)",
      "national_ranking": "string (optional - for existing locations)",
      "recreation_spending": "string (optional - for new locations)",
      "extracurricular_spending": "string (optional - for existing locations)",
      "summer_spending": "string (optional - for existing locations)",
      "poverty_rate": "string (optional - for new locations)",
      "child_poverty": "string (optional - for new locations)",
      "citations": "string"
    },
    "education_workforce": {
      "education_level": "string (optional - for new locations)",
      "workforce": "string (optional - for new locations)",
      "commute": "string (optional - for new locations)",
      "foreign_born": "string (optional - for new locations)",
      "citations": "string (optional - for new locations)"
    },
    "population_mobility": {
      "annual_net_influx": "string (optional - for new locations)",
      "annual_turnover": "string (optional - for new locations)",
      "citations": "string (optional - for new locations)"
    },
    "psychographics": {
      "lifestyle_description": "string (optional - for existing locations)",
      "weekend_activities": "string (optional - for existing locations)",
      "culture": "string (optional - for existing locations)",
      "health_orientation": "string (optional - for existing locations)",
      "diversity_inclusion": "string (optional - for existing locations)",
      "community_engagement": "string (optional - for existing locations)",
      "citations": "string (optional - for existing locations)"
    }
  },

  "competitor_analysis": {
    "direct_competitors": [
      {
        "name": "string",
        "location": "string",
        "distance": "string",
        "focus": "string",
        "pricing": "string",
        "facility_size": "string (optional - for existing locations)",
        "strengths": "string",
        "notes": "string (optional)",
        "citation": "string"
      }
    ],
    "other_notables": [
      {
        "name": "string (optional - for existing locations)",
        "location": "string (optional - for existing locations)",
        "type": "string (optional - for existing locations)",
        "model": "string (optional - for existing locations)",
        "relevance": "string (optional - for existing locations)",
        "citation": "string (optional - for existing locations)"
      }
    ],
    "market_insights": {
      "pricing_range": "string (optional - for new locations)",
      "competitive_assessment": "string (optional - for existing locations)",
      "capacity_constraints": "string (optional - for existing locations)",
      "franchise_positioning": "string (optional - for existing locations)",
      "ninja_differentiator": "string (optional - for existing locations)",
      "demand_indicators": "string (optional - for existing locations)",
      "willingness_to_pay": "string (optional - for new locations)",
      "market_gap": "string (optional - for new locations)",
      "citations": "string"
    }
  },

  "pricing_strategy": {
    "membership_tiers": [
      {
        "tier": "string",
        "price": "string",
        "description": "string (optional - for new locations)",
        "benchmark": "string"
      }
    ],
    "additional_pricing": {
      "sibling_discounts": "string (optional - for new locations)",
      "toddlers_only": "string (optional - for new locations)",
      "annual_membership_fee": "string (optional - for new locations)",
      "events_and_camps": "string (optional - for new locations)",
      "birthday_parties": "string (optional - for new locations)",
      "membership_model": "string (optional - for existing locations)",
      "family_discounts": "string (optional - for existing locations)",
      "free_trials": "string (optional - for existing locations)",
      "no_contracts": "string (optional - for existing locations)"
    },
    "price_strategy": {
      "hold_steady_2025": "string (optional - for existing locations)",
      "location_based_pricing": "string (optional - for existing locations)",
      "value_proposition": "string (optional - for existing locations)",
      "citations": "string (optional - for existing locations)"
    },
    "financial_projections": {
      "average_revenue_per_member": "string (optional - for new locations)",
      "digital_lead_costs": "string (optional - for existing locations)",
      "conversion_rate": "string (optional - for existing locations)",
      "customer_acquisition_cost": "string (optional - for existing locations)",
      "average_membership_duration": "string (optional - for new locations)",
      "customer_lifetime_value": "string (optional - for new locations)",
      "monthly_tuition": "string (optional - for existing locations)",
      "payback_period": "string (optional - for existing locations)",
      "lifetime_value": "string (optional - for existing locations)",
      "ltv_cac_ratio": "string (optional - for existing locations)",
      "target_cac": "string (optional - for new locations)",
      "cac_payback_period": "string (optional - for new locations)",
      "citations": "string"
    }
  },

  "swot_analysis": {
    "strengths": [
      {
        "item": "string",
        "description": "string",
        "citation": "string (optional)"
      }
    ],
    "weaknesses": [
      {
        "item": "string",
        "description": "string",
        "citation": "string (optional)"
      }
    ],
    "opportunities": [
      {
        "item": "string",
        "description": "string",
        "citation": "string (optional)"
      }
    ],
    "threats": [
      {
        "item": "string",
        "description": "string",
        "citation": "string (optional)"
      }
    ]
  },

  "real_estate_evaluation": {
    "location_details": {
      "address": "string",
      "square_footage": "string",
      "center_size": "string (optional - for new locations)",
      "building_type": "string (optional - for existing locations)",
      "zoning": "string (optional - for existing locations)",
      "previous_tenant": "string (optional - for new locations)"
    },
    "lease_terms": {
      "base_rent": "string",
      "nnn_estimate": "string (optional - for new locations)",
      "total_annual_rent": "string (optional - for existing locations)",
      "market_comparison": "string (optional - for existing locations)",
      "escalations": "string (optional - for existing locations)",
      "total_occupancy_cost": "string (optional - for new locations)",
      "occupancy_cost": "string (optional - for existing locations)",
      "escalator": "string (optional - for new locations)",
      "citation": "string"
    },
    "traffic_data": {
      "route_7": "string (optional - for Ashburn)",
      "ashburn_village_blvd": "string (optional - for Ashburn)",
      "snowden_river_parkway": "string (optional - for Columbia)",
      "visibility": "string (optional - for existing locations)",
      "speed": "string (optional - for existing locations)",
      "access": "string (optional - for existing locations)",
      "regional_access": "string (optional - for existing locations)",
      "citation": "string"
    },
    "co_tenants": [
      {
        "name": "string",
        "type": "string (optional)",
        "location": "string (optional)",
        "size": "string (optional)",
        "unit": "string (optional)",
        "role": "string",
        "relevance": "string (optional)",
        "impact": "string (optional)",
        "status": "string (optional)"
      }
    ],
    "area_amenities": {
      "nearby_retail": "string (optional - for existing locations)",
      "convenience": "string (optional - for existing locations)",
      "parking": "string (optional - for existing locations)",
      "citation": "string (optional - for existing locations)"
    }
  },

  "actionable_recommendations": [
    {
      "priority": "string",
      "action": "string",
      "description": "string",
      "owner": "string (optional - for new locations)",
      "timeline": "string",
      "rationale": "string (optional - for existing locations)",
      "expected_impact": "string (optional - for existing locations)",
      "kpis": [
        "string"
      ],
      "supporting_evidence": "string (optional)",
      "citation": "string (optional)"
    }
  ],

  "performance_benchmarks": {
    "internal_data": [
      {
        "location": "string",
        "opened": "string (optional)",
        "facility_size": "string (optional)",
        "lease_rate": "string (optional)",
        "market_size": "string (optional)",
        "peak_students": "string (optional)",
        "stable_students": "string (optional)",
        "current_students": "string (optional)",
        "current_enrollment": "string (optional)",
        "peak_enrollment": "string (optional)",
        "peak_revenue": "string (optional)",
        "avg_revenue": "string (optional)",
        "avg_membership": "string (optional)",
        "avg_membership_duration": "string (optional)",
        "monthly_tuition_range": "string (optional)",
        "waitlist": "string (optional)",
        "peak_waitlist": "string (optional)",
        "seasonal_pattern": "string (optional)",
        "notes": "string (optional)"
      }
    ],
    "projections_ashburn": {
      "year_1_target": "string (optional - for Ashburn)",
      "growth_strategy": "string (optional - for Ashburn)",
      "success_factors": "string (optional - for Ashburn)"
    },
    "projections_columbia": {
      "growth_target": "string (optional - for Columbia)",
      "duration_improvement": "string (optional - for Columbia)",
      "pricing_positioning": "string (optional - for Columbia)"
    }
  },

  "seasonal_considerations": {
    "summer_challenge": {
      "clarksburg_data": "string (optional - for new locations)",
      "pattern": "string (optional - for existing locations)",
      "mitigation_strategy": "string",
      "opportunity": "string (optional - for existing locations)",
      "citation": "string (optional)"
    },
    "growth_pattern": {
      "rapid_ramp": "string (optional - for new locations)",
      "waitlist_management": "string (optional - for new locations)"
    },
    "back_to_school_surge": {
      "pattern": "string (optional - for existing locations)",
      "strategy": "string (optional - for existing locations)",
      "citation": "string (optional - for existing locations)"
    }
  },

  "key_citations": {
    "location_sources": [
      {
        "id": "string (optional - for existing locations)",
        "source": "string (optional - for existing locations)",
        "data": "string (optional - for existing locations)"
      }
    ],
    "demographics_sources": [
      {
        "id": "string",
        "source": "string",
        "data": "string"
      }
    ],
    "competitor_sources": [
      {
        "id": "string",
        "source": "string",
        "data": "string"
      }
    ],
    "market_research_sources": [
      {
        "id": "string (optional - for existing locations)",
        "source": "string (optional - for existing locations)",
        "data": "string (optional - for existing locations)"
      }
    ],
    "internal_sources": [
      {
        "id": "string (optional - for new locations)",
        "source": "string (optional - for new locations)",
        "data": "string (optional - for new locations)"
      }
    ],
    "real_estate_sources": [
      {
        "id": "string (optional - for new locations)",
        "source": "string (optional - for new locations)",
        "data": "string (optional - for new locations)"
      }
    ]
  },

  "data_tables": {
    "children_population_7mi_radius": [
      {
        "location": "string (optional - for new locations)",
        "children_0_14": "number (optional - for new locations)"
      }
    ],
    "demographic_indicators": [
      {
        "indicator": "string (optional - for existing locations)",
        "value": "string (optional - for existing locations)",
        "7_mile_estimate": "string (optional - for existing locations)",
        "7_mile_value": "string (optional - for existing locations)",
        "county_wide": "string (optional - for existing locations)",
        "percentage": "string (optional - for existing locations)",
        "notes": "string (optional - for existing locations)",
        "description": "string (optional - for existing locations)",
        "breakdown": "string (optional - for existing locations)"
      }
    ],
    "performance_comparison": [
      {
        "location": "string (optional - for existing locations)",
        "facility_sqft": "string (optional - for existing locations)",
        "lease_rate": "string (optional - for existing locations)",
        "children_market": "string (optional - for existing locations)",
        "enrollment": "string (optional - for existing locations)",
        "waitlist": "string (optional - for existing locations)",
        "duration": "string (optional - for existing locations)",
        "tuition_range": "string (optional - for existing locations)"
      }
    ],
    "competitive_pricing": [
      {
        "provider": "string (optional - for existing locations)",
        "program": "string (optional - for existing locations)",
        "price": "string (optional - for existing locations)"
      }
    ],
    "ashbrook_commons_tenants": [
      {
        "tenant": "string (optional - for new locations)",
        "unit": "string (optional - for new locations)",
        "sq_ft": "string (optional - for new locations)",
        "role": "string (optional - for new locations)"
      }
    ]
  },

  "methodology_note": "string"
}
